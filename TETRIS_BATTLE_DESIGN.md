# 🧱⚔️ 테트리스 배틀 (Tetris Battle) 설계 문서

## 0. 확정 방향

- **형식**: 1v1 + 배틀로얄(최대 6~8인) 둘 다. 방 만들 때 선택.
- **봇**: 포함 (혼자서도 연습·플레이). 서버가 난이도별 추상 모델로 봇을 굴림.
- **피스 순서**: 독립 랜덤 (각자 다른 7-bag) — 정통 대전 방식.
- **실시간 방식**: **기존 폴링 구조 재사용**(WebSocket 불필요). 각자 브라우저에서 로컬로 게임을 돌리고, ~1초마다 공격량/보드요약을 서버에 보고하고 가비지·상대상황을 받아옴. 가비지 딜레이(~1초)는 정통 테트리스에도 있는 요소라 자연스러움.
- **재사용**: 솔로 테트리스의 `engine.ts` / `srs.ts` / 캔버스 렌더 / 모바일 조작 / RoomChat / 로비챗을 그대로 활용.

---

## 1. 핵심 규칙 — 공격(가비지) 시스템

줄을 지우면 상대에게 **방해 줄(가비지)**을 보냄. 정통 가이드라인 기준:

| 라인 클리어 | 보내는 가비지 |
|---|---|
| 싱글 (1줄) | 0 |
| 더블 (2줄) | 1 |
| 트리플 (3줄) | 2 |
| **테트리스 (4줄)** | 4 |
| T-스핀 싱글 | 2 |
| T-스핀 더블 | 4 |
| T-스핀 트리플 | 6 |
| 백투백(연속 테트리스/T스핀) | +1 |
| 퍼펙트 클리어 | +10 (또는 고정 대량) |

**콤보 보너스** (연속으로 줄을 지울 때, 단계별 누적):
| 콤보 | +가비지 |
|---|---|
| 1~2 | +1 |
| 3~4 | +2 |
| 5~6 | +3 |
| 7+ | +4 |

### 1.1 상쇄(Offset / Counter)
가비지는 즉시 안 박히고 **대기열(pending)**에 쌓임. 다음 조각을 놓을 때:
- **줄을 지웠으면** → 내 공격이 먼저 **대기 중인 가비지를 상쇄**함. 남은 공격만 상대에게 전송.
- **줄을 못 지우고 조각을 놓으면** → 그때 대기 가비지가 내 바닥으로 **올라옴**(박힘).

즉 "받은 만큼 되받아치면 안 맞는다". 이게 배틀의 핵심 심리전.

### 1.2 가비지 모양
- 바닥에서 **구멍 한 칸 뚫린 줄**로 올라옴.
- 연속 가비지는 구멍 열이 **같게 유지**되다가 가끔 바뀜(정통 규칙: 다 뚫린 구멍 정렬).

### 1.3 타겟팅 (배틀로얄)
- **1v1**: 공격 → 유일한 상대.
- **배틀로얄**: 내 공격은 서버가 **생존 중인 상대 한 명에게** 배정.
  - v1 규칙: **랜덤 생존자**(자신 제외). (확장: '가장 높이 쌓인 상대'·'날 때린 상대에게 반격' 등은 2차)

---

## 2. 승패

- **탑아웃**: 가비지·낙하로 블록이 꼭대기(스폰 자리)까지 차면 **KO(탈락)**.
- **1v1**: 상대를 먼저 KO 시키면 승리.
- **배틀로얄**: **최후 생존 1인 = 우승.** 탈락 순서대로 등수(꼴찌부터 KO). 마지막 남은 사람이 1등.
- **안전장치**: 아무도 안 죽고 교착되면 최대 시간(예: 5분) 후 **가장 적게 쌓인 사람 승**(또는 라인 수 기준).

---

## 3. 아키텍처 (폴링 기반 — 기존 인프라 재사용)

### 3.1 큰 그림
```
[브라우저 A]  로컬로 자기 테트리스 실행(engine.ts)
     │  매 ~1초 / 조각 놓을 때마다
     │   POST /sync  { 내가 보낸 공격량, 보드요약, 생존여부, KO한 상대 }
     ▼
[서버 TetrisBattleGame]  가비지 중계 + 생존 집계 (전체 보드 검증 X, 클라 신뢰)
     │   각 플레이어별: 받을 가비지 큐, 상대 요약, 생존자 수, 페이즈
     ▲
     │  POST /sync 응답 / GET /me
[브라우저 A]  받은 가비지를 대기열에 넣고, 상대 미니보드 갱신
```
- 서버는 **공격량(숫자)만 라우팅**하고 보드 자체는 안 들고 있음 → 가벼움.
- 다른 멀티 게임들처럼 **클라 신뢰**(친구용 캐주얼이라 치팅 검증 생략).

### 3.2 백엔드 (신규 `com.wordplay.tetris`)
- `TetrisBattleGame implements RoomGame` — 방 하나. 플레이어(사람+봇), 페이즈(LOBBY/PLAYING/ENDED), 각 플레이어 상태(생존, 받을 가비지 큐, 보드요약, 보낸 라인 수, 등수).
- `TetrisBotSim` — 서버측 봇 추상 모델(아래 4장).
- `TetrisBattleController` — REST 엔드포인트.
- `RoomRegistry<TetrisBattleGame>` 재사용. `RoomSummary`로 방 목록.

### 3.3 엔드포인트
| 메서드 | 경로 | 용도 |
|---|---|---|
| POST | `/api/tetris-battle/create` | 방 생성 `{nick, clientId, format, koTarget}` → `{roomCode, state}` |
| POST | `/api/tetris-battle/join` | 참가 `{roomCode, nick, clientId}` |
| POST | `/api/tetris-battle/add-bot` | 봇 추가(방장) `{roomCode, clientId, difficulty}` |
| POST | `/api/tetris-battle/start` | 시작(방장) |
| POST | `/api/tetris-battle/sync` | 플레이 중 보고+수신 `{roomCode, clientId, attacks, boardSummary, alive, koOf}` → 최신 상태 |
| GET  | `/api/tetris-battle/me` | 상태 조회(로비 폴링·읽기용) |
| POST | `/api/tetris-battle/leave` | 나가기 |
| GET  | `/api/tetris-battle/rooms` | 방 목록 |

### 3.4 sync 응답(플레이 중 상태) 형태
```jsonc
{
  "phase": "PLAYING",
  "me":  { "incomingGarbage": 3, "alive": true, "placement": null },
  "opponents": [
    { "name": "라미", "bot": false, "alive": true, "heights": [2,3,3,5,4,4,2,1,0,0], "danger": false },
    { "name": "봇1",  "bot": true,  "alive": false, "heights": [], "danger": false }
  ],
  "aliveCount": 2,
  "winner": null   // 종료 시 승자 이름
}
```
- `heights`: 10칸 열 높이 배열(미니보드 렌더용). `boardSummary`도 클라→서버 동일 형태.
- `incomingGarbage`: 서버가 나에게 배정한 누적 가비지(클라가 받아 대기열에 병합, 상쇄 처리).

---

## 4. 서버측 봇 (추상 모델)

서버가 진짜 테트리스 AI를 60fps로 돌리는 건 과함. **추상 모델**로 가볍게:

각 봇(난이도별 파라미터):
- `clearRate` — 초당 지우는 줄 수(공격 생산 속도).
- `attackEff` — 클리어가 더블·테트리스일 확률(공격 효율).
- `defense` — 들어온 가비지를 지워 없애는 능력(상쇄율).
- `stack` — 현재 쌓인 높이(가상). 가비지 받으면 오르고, 클리어하면 내려감. 임계 초과 시 **KO**.

**틱(매 ~1초)**:
1. `clearRate`만큼 줄 클리어 누적 → `attackEff`로 가비지량 산출 → 타겟에게 전송.
2. 받은 가비지: `defense`로 일부 상쇄, 나머지 `stack`에 반영.
3. `stack`이 임계 초과 → 봇 KO.
4. 미니보드용 `heights`: `stack` 기반으로 그럴듯한 높이 프로필 생성.

**난이도 (오목과 동일하게 초·중·고급 3단계)**:
| 난이도 | clearRate | attackEff | defense |
|---|---|---|---|
| 초급 | 낮음 | 낮음 | 낮음 |
| 중급 | 보통 | 보통 | 보통 |
| 고급 | 높음 | 높음(테트리스 잦음) | 높음 |

→ 사람 눈엔 "봇이 공격을 보내오고, 몰아치면 죽는" 걸로 보임. 실제 보드가 없어도 체감 OK.

---

## 5. 클라이언트 (기존 engine.ts 확장)

### 5.1 engine.ts 추가 기능
- `addGarbage(rows, holeCol)` — 바닥에 구멍 뚫린 가비지 줄 삽입(위로 밀어 올림). 꼭대기 넘치면 top-out.
- `attackFromResult(res)` — `lock()` 결과(라인수/T스핀/백투백/콤보/퍼펙트)로 **보낼 가비지량** 계산(1장 표).
- `columnHeights()` — 10칸 열 높이 배열(보드요약).

### 5.2 페이지 `/tetris-battle` 가비지/상쇄 로직
- `pendingGarbage`(대기 가비지) 상태 유지.
- 서버에서 받은 `incomingGarbage` 증가분을 `pendingGarbage`에 병합.
- 조각 놓을 때(lock):
  - 공격 A = `attackFromResult`.
  - A>0: `cancel = min(A, pendingGarbage)`; 대기 차감; 남은 `A-cancel`을 **다음 sync에 실어 서버로**.
  - A==0 & pendingGarbage>0: `addGarbage(pendingGarbage)` 후 0으로.
- KO(top-out) 감지 → `alive=false`로 sync, 관전 모드 전환.

### 5.3 화면 (반응형)
```
┌ 내 보드(캔버스) ┬ 받을가비지 게이지 ┐   [상대 미니보드들]
│                │  ▓▓▓ (경고 시 빨강) │   ┌──┐ ┌──┐ ┌──┐
│                │                  │   │라미│ │봇1│ │봇2│
│  HOLD / NEXT    │                  │   └──┘ └──┘ └──┘
└────────────────┴──────────────────┘   생존 3/6
```
- **받을 가비지 게이지**: 대기 가비지가 쌓이면 옆에 빨간 바로 경고(곧 올라옴).
- **상대 미니보드**: `heights`로 작게 렌더(색 없이 실루엣). 위험(높이 높음)하면 빨강 테두리.
- **공격/피격 연출**: 공격 보낼 때 상대 쪽으로 이펙트, 맞을 때 흔들림.
- 모바일: 솔로 조작바 재사용, 상대 미니보드는 상단 가로 스크롤.

### 5.4 로비
- 방 목록(코드로 참가) + 방 생성(형식/승리조건 선택).
- 방장: 봇 추가(난이도), 시작. 플레이어 목록.
- **로비챗 + 방 채팅(RoomChat)** 재사용.

---

## 6. 홈/진입

- 홈 게임 목록에 별도 카드로 넣거나, 기존 🧱 테트리스 카드에서 **"랭킹전 / 배틀"** 선택 진입.
- 배틀은 `solo:true`(봇 포함이라 혼자도 가능)로 표기.

---

## 7. 남은 결정 사항(구현 중 확정)

1. **최대 인원**: 배틀로얄 6인 vs 8인 (미니보드 가독성 고려 — 6 추천).
2. **타겟팅 규칙**: v1 랜덤 생존자 — OK? (2차에 '위험한 상대 우선' 등)
3. **교착 안전장치 시간**: 5분? 무제한?
4. **배틀 랭킹**: 승수/우승 기록을 남길지(별도 `tb_score` 키 `tetris_battle` = 우승 횟수). v1은 생략하고 판별 재미만, 2차에 추가 가능.

---

## 8. 구현 순서(마일스톤)

1. **엔진 확장**: `addGarbage` / `attackFromResult` / `columnHeights` + 단위 테스트(가비지 삽입·공격량·상쇄).
2. **백엔드 방**: `TetrisBattleGame`(로비·시작·플레이·종료) + 컨트롤러 + RoomRegistry. 사람만 1v1 먼저.
3. **프론트 1v1**: 로컬 엔진 + sync 폴링 + 가비지/상쇄 + 상대 미니보드. 사람 대 사람 1v1 완성.
4. **배틀로얄**: 다인 + 타겟팅 + 생존/등수 + 최후 1인 판정.
5. **봇(TetrisBotSim)**: 추상 모델 3난이도 + 미니보드 프로필. 혼자 플레이 지원.
6. **연출·모바일 QA**: 공격/피격 이펙트, 경고 게이지, 미니보드 모바일 레이아웃.
7. (선택) **배틀 랭킹**(우승 횟수) + 홈 진입 정리.
