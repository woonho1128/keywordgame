# AI 사주 설계안

## 1. 개요

생년월일시로 **사주팔자를 서버에서 계산**하고, 계산 결과를 **AI가 해석**해주는 기능이다.

WordPlay의 게임들과 달리 승패나 리더보드가 없고, 대신 기존 흐름 중 "입력 → 결과 → 공유 URL"만 가져다 쓴다. 결과는 `/saju/{readingId}`로 다시 열어볼 수 있다.

### 1.1 핵심 결정: 계산과 해석의 분리

| 영역 | 담당 | 이유 |
|------|------|------|
| 사주팔자(연·월·일·시주), 오행, 십성, 대운, 세운 | **Java (SajuCalculator)** | LLM은 60갑자·절기 계산을 자주 틀린다. 결정론적이고 테스트 가능해야 한다 |
| 해석, 조언, 문장 생성 | **AI (OpenAI Chat)** | 계산이 아니라 글쓰기 영역 |

프롬프트에는 계산 결과를 "이미 확정된 사실"로 못 박고, 다시 계산하지 말라고 지시한다.

### 1.2 기능

| 기능 | 경로 | 설명 |
|------|------|------|
| 사주 | `/saju` | 한 사람의 사주팔자를 7가지 주제로 풀이 |
| 궁합 | `/saju/compat` | 두 사람의 사주를 맞대어 5가지 관계로 풀이 |

### 1.3 사주 종류

| 코드 | 이름 | 관점 |
|------|------|------|
| `TOTAL` | 종합사주 | 타고난 기질, 강약점, 인생 전반의 흐름 |
| `LOVE` | 연애사주 | 연애 성향, 맞는 상대, 인연의 흐름 |
| `WEALTH` | 재물사주 | 재성 중심 — 돈 버는 방식, 지출 성향 |
| `CAREER` | 직업사주 | 관성·식상 중심 — 적성과 커리어 |
| `STUDY` | 공부사주 | 인성 중심 — 학습 스타일, 시험운 |
| `HEALTH` | 건강사주 | 오행 과부족 — 체질, 생활 습관 |
| `YEARLY` | 올해의 운세 | 세운 + 현재 대운 |

종류가 늘어나도 `SajuType` enum에 항목 하나만 추가하면 된다.
label/emoji/설명/프롬프트 관점/섹션 주제/요점 항목이 모두 enum에 붙어 있고,
DB에도 종류 CHECK 제약을 걸지 않는다(종류를 늘릴 때마다 마이그레이션이 필요해지고,
빠뜨리면 운영에서 INSERT가 깨진다. 검증은 Java enum이 한다).

### 1.4 궁합 종류

| 코드 | 이름 | 관점 |
|------|------|------|
| `LOVE` | 연인궁합 | 끌림의 방향, 다투는 지점 |
| `COUPLE` | 부부궁합 | 일지(배우자 자리) 중심, 생활 리듬과 재물 |
| `FRIEND` | 친구궁합 | 부담 없이 오래 가는 사이인지 |
| `WORK` | 동료궁합 | 역할 분담, 동업의 위험 지점 |
| `FAMILY` | 가족궁합 | 가까운 만큼 부딪치는 부분 |

`CompatType` enum 하나로 관리하는 구조도 사주와 같다.

## 2. 사주팔자 계산 규칙

### 2.1 연주 — 입춘 기준

1월 1일이 아니라 **입춘**에 해가 바뀐다. 입춘 전 출생은 전년도 간지를 쓴다.

서기 4년이 갑자년이므로 `(연도 - 4) mod 60`.

### 2.2 월주 — 12절 기준

달력 월이 아니라 **절기**로 월지가 정해진다.

| 절기 | 월지 | 절기 | 월지 |
|------|------|------|------|
| 입춘 | 인(寅) | 입추 | 신(申) |
| 경칩 | 묘(卯) | 백로 | 유(酉) |
| 청명 | 진(辰) | 한로 | 술(戌) |
| 입하 | 사(巳) | 입동 | 해(亥) |
| 망종 | 오(午) | 대설 | 자(子) |
| 소서 | 미(未) | 소한 | 축(丑) |

월간은 년간에서 **오호둔(五虎遁)**으로 유도한다.

- 갑·기년 → 병인월 / 을·경년 → 무인월 / 병·신년 → 경인월 / 정·임년 → 임인월 / 무·계년 → 갑인월

### 2.3 절입 시각 계산

근사식(寿星공식) 대신 **태양의 겉보기 황경**을 직접 계산한다.

- Meeus 저정밀 태양 위치 공식 → 황경이 목표 각도(입춘 315°, 경칩 345°, …)가 되는 순간을 이분 탐색
- 오차: 황경 약 0.01도 ≈ 시간으로 15분
- 근사식을 안 쓰는 이유: 근사식은 **베이징 기준 "일" 단위**라 한국시(UTC+9)에서 자정 근처 절입이 하루씩 어긋난다
  - 예: 2024년 대설은 KST 12/7 00:17 (근사식은 12/6으로 계산)
- ΔT(역학시-세계시 차이, 현대 기준 약 70초)는 무시한다

지원 범위는 **1901년 ~ 2099년**.

### 2.4 일주 — 율리우스일

`(epochDay + 2440637) mod 60` (0 = 갑자).

검증 기준점: **1949-10-01 = 갑자일**.

### 2.5 시주

- 지지: 23시~01시 자시, 01~03시 축시 … 2시간 단위
- 천간: 일간에서 **오자둔(五鼠遁)** — 갑·기일 갑자시 / 을·경일 병자시 / 병·신일 무자시 / 정·임일 경자시 / 무·계일 임자시
- **23시 이후 출생은 다음 날 일주**로 본다 (야자시/조자시는 구분하지 않는다)

### 2.6 진태양시 보정

한국 표준시는 동경 135도 기준인데 서울은 약 127도다. 실제 태양시보다 약 30분 빠르므로 **-30분 보정**한다.

`app.saju.longitude-offset-minutes` 로 조정 가능하고, `0`이면 보정하지 않는다. 시주 경계에만 영향을 준다.

### 2.7 대운

- 방향: **양남음녀 순행, 음남양녀 역행** (년간의 음양 + 성별)
- 대운수: 순행이면 다음 절입까지, 역행이면 지난 절입부터의 날수를 3으로 나눈다 (나머지 1은 버림, 2는 올림, 최소 1)
- 간지: 월주에서 순행/역행으로 한 칸씩, 10년 단위 8개

### 2.8 한계 (알고 쓰는 부분)

- **양력만 지원**. 음력 생일은 사용자가 양력으로 변환해서 입력해야 한다
- 절입 시각 오차 약 15분 — 절입 순간 15분 이내 출생이면 월주가 갈릴 수 있다
- 1908~1961년 사이 한국 표준시 변경 이력은 반영하지 않는다
- 지장간은 정기(正氣)만 쓴다 (십성 판정용)

## 2-2. 궁합 계산 규칙

두 사람의 사주를 뽑은 뒤, 자리별로 관계를 판정한다. 판정은 전부 결정론적이라 같은 두 사람이면 점수도 항상 같다.

### 2-2.1 천간 관계

| 관계 | 내용 |
|------|------|
| 합(合) | 갑기합토 · 을경합금 · 병신합수 · 정임합목 · 무계합화 (인덱스 차 5) |
| 충(沖) | 갑경 · 을신 · 병임 · 정계 (무·기는 충하지 않는다) |

### 2-2.2 지지 관계

| 관계 | 내용 | 판정 |
|------|------|------|
| 육합 | 자축·인해·묘술·진유·사신·오미 | 두 인덱스 합 ≡ 1 (mod 12) |
| 삼합 | 신자진(수)·해묘미(목)·인오술(화)·사유축(금) | 인덱스 % 4 가 같다 |
| 충 | 자오·축미·인신·묘유·진술·사해 | 인덱스 차 6 |
| 형 | 인사신·축술미 삼형, 자묘 상형, 진진·오오·유유·해해 자형 | 목록 |
| 해 | 자미·축오·인사·묘진·신해·유술 | 두 인덱스 합 ≡ 7 (mod 12) |
| 파 | 자유·축진·인해·묘오·사신·미술 | 목록 |

한 쌍이 여러 관계에 겹칠 수 있다 (인해는 육합이면서 파). 수식으로 구현한 것들은 `RelationTest`에서 12×12 전 조합을 돌려 전통 목록과 정확히 일치하는지 확인한다.

### 2-2.3 점수

기본 62점에서 시작해 자리별 가중치를 더한다.

| 자리 | 배율 | 근거 |
|------|------|------|
| 일지 | 1.0 | 배우자 자리 — 궁합의 핵심 |
| 연지 | 0.5 | 띠 궁합 |
| 월지 | 0.5 | 사회적 기질 |
| 시지 | 0.3 | 가장 약하게 본다 |

관계별 기본값(일지 기준): 육합 +14, 삼합 +11, 충 −13, 형 −8, 해 −6, 파 −4.
여기에 일간 천간합 +12 / 천간충 −9, 십성 관계(정 계열 +5, 비견 +3, 겁재·상관·편관 −3),
오행 보완(상대가 채워주는 오행 1개당 +4, 최대 +12)을 더하고 30~98로 자른다.

> 가중치 숫자는 명리 고전에 정해진 값이 아니라 이 서비스의 기준이다. 점수는 재미 요소고,
> 실제 해석에서 중요한 건 **어떤 관계가 어느 자리에 걸렸는지**다. A와 B를 바꿔 넣어도 점수는 같다.

## 3. 데이터 모델

### 3.1 TB_SAJU_READING

| 컬럼 | 타입 | 설명 |
|------|------|------|
| `reading_id` | VARCHAR(12) | nanoid, 공유 URL에 사용 |
| `saju_type` | VARCHAR(20) | TOTAL / LOVE / WEALTH / CAREER / STUDY / HEALTH / YEARLY |
| `nickname` | VARCHAR(20) | 선택 |
| `birth_date` | DATE | 양력 |
| `birth_time` | TIME | 모르면 NULL |
| `time_unknown` | BOOLEAN | 시주 제외 여부 |
| `gender` | VARCHAR(10) | MALE / FEMALE (대운 방향 판정) |
| `chart_json` | JSONB | **서버가 계산한** 사주팔자/오행/대운/세운 |
| `result_json` | JSONB | **AI가 생성한** 해석 |
| `ai_model` | VARCHAR(60) | 어떤 모델로 뽑았는지 |
| `cache_key` | VARCHAR(64) | 입력값 SHA-256 |
| `view_count` | INTEGER | 조회수 |

기존 DB에는 `db/saju_migration.sql`을 실행한다.

### 3.2 TB_SAJU_COMPAT

| 컬럼 | 설명 |
|------|------|
| `compat_id` | nanoid, 공유 URL |
| `compat_type` | LOVE / COUPLE / FRIEND / WORK / FAMILY |
| `a_*`, `b_*` | 두 사람의 닉네임·생년월일·시각·성별 |
| `score` | 계산된 궁합 점수 |
| `analysis_json` | **서버가 계산한** 두 사주 + 자리별 합·충 근거 |
| `result_json` | **AI가 생성한** 해석 |

`db/saju_migration.sql` 하나에 사주·궁합 테이블이 모두 들어 있고 `CREATE TABLE IF NOT EXISTS`라
이미 적용한 DB에 다시 실행해도 안전하다.

### 3.3 동일 입력 재사용

`cache_key = SHA-256(종류|닉네임|생년월일|출생시각|성별|기준연도|프롬프트버전)`

사주는 바뀌지 않으므로 같은 입력이면 이전 해석을 그대로 돌려준다. AI 호출 비용도 아낀다.

**기준 연도를 키에 넣는 이유**: 나이와 세운은 해가 바뀌면 달라진다. 안 넣으면 "올해의 운세"가 내년에도 작년 해석을 돌려준다.

**프롬프트 버전을 키에 넣는 이유**: 해석 형식을 바꿔도 캐시가 살아 있으면 예전 형식의
짧은 해석이 계속 나온다. 프롬프트나 응답 스키마를 바꿀 땐 `SajuService.PROMPT_VERSION`
(궁합은 `SajuCompatService.PROMPT_VERSION`)을 올려야 새 형식으로 다시 뽑는다.

`app.saju.reuse-same-input: false` 로 끌 수 있다. 궁합도 같은 방식이며, A와 B의 순서를
바꿔 넣어도 같은 결과를 쓰도록 두 사람 정보를 정렬해서 해싱한다.

## 4. AI 호출

### 4.1 모델

`SajuAiClient` — WordSim 임베딩·마피아 봇과 **API 키(`app.openai.api-key`)만 공유**하고, 모델과 타임아웃은 `app.saju.chat-*` 로 따로 둔다. 기본 모델은 `gpt-4o-mini` (사주 1건당 대략 $0.001 미만).

> 마피아 봇에도 `com.wordplay.mafia.ai.OpenAiChatClient` 가 있다. 클래스 이름이 같으면 스프링 빈 이름이 겹쳐 앱이 아예 안 뜨므로 사주 쪽은 `SajuAiClient` 로 둔다. 설정 키도 `app.openai.chat-*`(마피아)와 `app.saju.chat-*`(사주)로 분리했다 — 같은 키를 쓰면 한쪽 모델을 바꿀 때 다른 쪽까지 끌려간다.

`app.saju.chat-url` 을 바꾸면 OpenAI 호환 게이트웨이로도 붙일 수 있다. 마피아 봇과 같은 모델을 쓰고 싶으면 `SAJU_CHAT_MODEL=gpt-5.6-luna`.

### 4.2 응답 스키마

`response_format: json_object` 로 JSON을 강제하고, 아래 구조로 받는다.

```json
{
  "headline": "한 줄 총평",
  "summary": "전체 요약 (5~6문장)",
  "highlights": [{ "label": "앞으로 만날 인연", "value": "3번", "detail": "그렇게 본 근거" }],
  "encounters": [{ "year": "2027년 정미년", "past": false, "where": "일터·업무 모임",
                   "story": "뭐 하다 만나는지",
                   "partner": { "age": "또래~2살 연상", "job": "기획·마케팅 쪽", "vibe": "성격 한 줄" },
                   "basis": "사주 근거" }],
  "sections": [{ "title": "소제목", "body": "본문 (5~8문장)" }],
  "strengths": ["타고난 강점"],
  "cautions": ["조심할 점"],
  "timeline": [{ "period": "30~39세 무신 대운", "body": "그 시기의 흐름" }],
  "keywords": ["키워드"],
  "lucky": { "color": "", "number": "", "direction": "", "item": "" },
  "advice": "실천 조언",
  "score": 77
}
```

`highlights`는 결과 맨 위에 한눈에 보여주는 요점 카드다. 무엇을 뽑을지는 종류마다
`SajuType.highlightHint()`로 지정한다 (미래인연이면 횟수·자리·시기).

`encounters`는 미래인연 전용 연표다. 지난 기회 2개 + 앞으로 3개를 연도·장소·장면·상대 인상으로 받는다.

> **연도를 지어내지 못하게 한다.** 프롬프트에 [연도별 세운] 표(만 16세 ~ 기준연도+12년,
> 각 해의 간지와 십성)를 계산해 넣고, 그 표 안에서만 고르게 한다. 연주 계산은
> `SajuCalculator.yearPillarOf()` 하나를 공유해 규칙이 갈라지지 않게 했다.

> **상대가 어떤 사람인지도 계산에서 끌어온다.** 명리에서 배우자는 남자는 재성(財星),
> 여자는 관성(官星)으로 본다. 그 별이 사주 어느 자리에 앉았고 오행이 무엇인지 계산해
> 프롬프트에 넣는다 (`appendSpouseStar`). 나이대는 자리(연주·월주 쪽이면 연상, 시주 쪽이면
> 연하), 직업의 결은 오행, 성격은 일지의 십성에서 읽는다. AI가 상상으로 지어내지 않게 하려는 것.

> **답은 생활 언어로.** "인성의 자리" 같은 명리 용어는 "어디서 만나나"의 답이 되지 못한다.
> 장소는 "일터·업무 모임", "친구 소개 자리"처럼 실제로 갈 수 있는 곳으로 쓰게 시스템 프롬프트에
> 못박았다. 사주 용어는 근거를 설명할 때만 쓴다.

섹션은 종류별로 6개를 요구한다. 궁합은 `lucky`/`timeline`/`score` 대신 `aToB`·`bToA`(서로에게
해주면 좋은 것)를 받고, 점수는 서버 계산값을 그대로 쓴다.

모델이 스키마를 조금 벗어나도 깨지지 않게 모르는 필드는 무시하고, `summary`와 `sections`가 비면 실패로 처리한다.
`highlights`·`strengths`·`cautions`·`timeline`은 나중에 추가된 필드라 그전에 저장된 해석에는
없다 — 화면에서 null을 처리한다.

### 4.2-1 분량과 토큰

한국어는 토큰을 많이 먹는다. 6개 섹션 + 강점/주의점/시기별 흐름까지 받으면 2000 토큰으로는
중간에 잘리고, 잘리면 JSON이 깨져 통째로 실패한다. 그래서 기본값을 6000으로 둔다
(`SAJU_CHAT_MAX_TOKENS`). 응답의 `finish_reason`이 `length`면 경고 로그를 남긴다.

### 4.3 모델 호환 재시도

- `400` + `max_completion_tokens` 언급 → `max_tokens`를 바꿔 재시도 (최신 모델 대응)
- `400` + `temperature` 언급 → temperature 제거 후 재시도
- `429` / `5xx` → 짧게 쉬고 재시도 (최대 3회)
- `401` 등 그 외 → 즉시 실패

### 4.4 호출 한도

설계서 v1.1에서 게임의 Rate Limit은 걷어냈지만, 사주는 호출마다 실제 비용이 나가므로 최소한의 방어를 둔다.

- 세션당 하루 10건 (`app.saju.limit-per-session`)
- 전체 하루 300건 (`app.saju.limit-total`)

Redis 없이 in-memory 카운터. 서버 재시작 시 초기화된다. 캐시 적중은 AI를 부르지 않으므로 세지 않고, AI 호출이 실패하면 카운터를 되돌린다.

## 5. API

| Method | Path | 설명 |
|--------|------|------|
| GET | `/api/v1/saju/types` | 사주 종류 목록 + 사용 가능 여부 |
| POST | `/api/v1/saju` | 사주 보기 (AI 해석 생성) |
| GET | `/api/v1/saju/{readingId}` | 공유 URL로 다시 보기 |
| GET | `/api/v1/saju/compat/types` | 궁합 종류 목록 |
| POST | `/api/v1/saju/compat` | 궁합 보기 (AI 해석 생성) |
| GET | `/api/v1/saju/compat/{compatId}` | 궁합 결과 다시 보기 |

`OPENAI_API_KEY`가 없으면 `types.available = false`가 되고, 생성 요청은 `SAJU_AI_UNAVAILABLE(503)`로 응답한다.

## 6. 화면

| 경로 | 설명 |
|------|------|
| `/saju` | 종류 선택 + 생년월일시·성별 입력 |
| `/saju/{readingId}` | 결과 — 총평/점수/사주팔자표/해석 6섹션/강점·주의점/시기별 흐름/행운/조언 + 공유 |
| `/saju/compat` | 궁합 종류 선택 + 두 사람 입력 |
| `/saju/compat/{compatId}` | 궁합 결과 — 점수/자리별 합·충 근거/해석/서로에게 해줄 것 + 공유 |

### 6.1 이미지 저장

결과 화면의 "이미지로 저장"은 **화면을 캡처하지 않고 결과 전체를 캔버스에 새로 그린다**
(`lib/sajuCard.ts`, 가로 1080 고정 · 세로는 내용만큼).

- DOM 캡처(html2canvas 류)를 안 쓰는 이유: 의존성이 늘고, 버튼·링크·접힌 영역까지 같이 찍힌다.
  한글은 캔버스가 시스템 폰트로 그려주므로 폰트 파일도 필요 없다
- **결과 전체**를 담는다 — 사주: 총평·점수·사주팔자(십성 포함)·오행·십성 분포·키워드·요약·
  해석 6섹션·강점·주의점·시기별 흐름·행운·조언.
  궁합: 총평·점수·자리별 합충 근거·키워드·요약·해석 5섹션·잘 맞는/부딪치는 부분·
  서로에게 해줄 것·조언
- **높이는 내용에 맞춰 늘어난다.** 먼저 1px 캔버스에 본문을 그려보며 높이만 재고(그리기는 잘린다),
  그만큼의 캔버스를 만들어 다시 그린다 (`renderCard`). 큰 임시 캔버스를 잡지 않으려는 방법이다.
  실제 길이는 사주 약 4900px, 궁합 약 3400px
- **모바일**(`pointer: coarse`)은 공유 시트를 띄워 카톡으로 바로 보낸다.
  `toBlob`(비동기) 콜백에서 `navigator.share`를 부르면 iOS가 제스처가 아니라며 막으므로
  `toDataURL`(동기)로 뽑아 쓴다
- **PC**는 공유를 아예 쓰지 않고 곧바로 파일로 내려받는다. 데스크톱 크롬도 Web Share를
  지원해서, 분기하지 않으면 윈도우 공유창이 떠 "저장이 안 된다"처럼 보인다
- 분기 로직은 `lib/saveImage.ts` 로 분리했다 (기기마다 갈려서 따로 검증하는 편이 낫다)

사주팔자 표는 천간·지지를 오행 색으로 칠하고 십성을 함께 보여준다. "서버 계산 · AI 추측 아님" 라벨로 해석과 구분한다.

카카오톡 공유 미리보기를 위해 `/saju/[readingId]/layout.tsx`에서 Open Graph 메타태그를 서버에서 생성한다.

## 7. 면책

재미로 보는 콘텐츠임을 입력 화면과 결과 화면 양쪽에 표시한다. 프롬프트에서도 질병 진단·투자 권유·법률 판단을 확정적으로 말하지 않도록 지시한다.
