# 쿠(Coup) 게임 설계서

> **게임명:** 쿠 (Coup)
> **작성:** 2026-07-13
> **개발 브랜치:** `claude/spyfall-mafia-game-url-ks6y07`

---

## 1. 개요

블러핑·추리 카드게임. 각자 정체(영향력 카드 2장)를 숨긴 채 서로 속이고 의심한다. 마지막까지 카드가 남은 1인이 승리.

### 1.1 확정 사항
| 항목 | 결정 |
|---|---|
| 인원 | **2~6인** |
| 봇 | **포함**(혼자 연습 가능, 난이도 추후) |
| 반응 제한시간 | 기본 **15초** (방 생성 시 8/15/25 선택 가능) |
| 카드 | **기본 5종**(공작·암살자·대장·대사·백작부인) 15장. 확장은 추후 |

- 기존 인프라(방 + `clientId` + 인메모리 `RoomRegistry` + 폴링 + 봇) 재사용.

---

## 2. 구성물 · 시작 세팅

- 덱: 5종 × 3장 = **15장**.
- 각 플레이어: **영향력 카드 2장(뒷면)** + **코인 2개**.
- 남은 카드는 덱(더미)로 둔다. 남의 카드는 안 보이고, **아무 카드나 있다고 주장(블러핑) 가능**.

---

## 3. 카드 & 능력

| 카드 | 액션 | 차단 |
|---|---|---|
| 🎩 공작 Duke | **세금** — 코인 +3 | 해외원조 차단 |
| 🗡️ 암살자 Assassin | **암살** — 코인 3 지불, 대상 카드 1장 잃음 | — |
| ⚓ 대장 Captain | **강탈** — 대상 코인 2개 뺏음 | 강탈 차단 |
| 🎭 대사 Ambassador | **교환** — 덱에서 2장 뽑아 손패 교체 | 강탈 차단 |
| 👒 백작부인 Contessa | (액션 없음) | 암살 차단 |

---

## 4. 액션 (턴마다 1개)

### 일반(카드 불필요)
- **소득 Income**: 코인 +1. (반응 불가, 안전)
- **해외원조 Foreign Aid**: 코인 +2. (공작 주장으로 **차단** 가능)
- **쿠 Coup**: 코인 **7 지불** → 대상 카드 1장 제거. (**의심·차단 불가**) · 코인 **10개 이상이면 강제 쿠**.

### 캐릭터(카드 주장 → 의심 가능)
- **세금**(공작) / **암살**(암살자, 코인 3) / **강탈**(대장) / **교환**(대사)

---

## 5. 핵심 메커니즘 — 의심 & 차단

- **의심 Challenge**: 캐릭터 액션·차단 주장에 다른 플레이어가 도전.
  - 주장자가 실제 그 카드 보유 → **도전자**가 카드 1장 잃음. 주장자는 그 카드를 덱에 섞고 새로 1장 뽑음(정체 유지).
  - 뻥이면 → **주장자**가 카드 1장 잃고 액션 무효.
- **차단 Block**: 대상(해외원조는 전원)이 차단 카드를 주장해 막음. 이 차단도 **의심 가능**.
- **암살 코인 처리**: 암살은 선언 시 3코인 지불. 백작부인 차단이 성공하면 코인은 **환불 안 됨**(암살은 일어났고 막힌 것). 단, 암살자 주장이 의심으로 뻥 판명되어 액션이 취소되면 **환불**.

---

## 6. 상태머신 (구현의 핵심)

폴링 구조에서 **"반응 창"**(다른 사람의 의심/차단 대기)을 처리하기 위해, 턴을 서브 스텝으로 나눈다. 각 대기 스텝은 **제한시간 자동패스**를 가진다.

### 6.1 스텝
```
CHOOSE_ACTION     현재 플레이어가 액션 선택            (현재 턴 대기)
CHALLENGE_ACTION  캐릭터 액션 주장 → 나머지 의심/통과   (반응 창)
BLOCK_ACTION      차단 가능 액션 → 대상(전원) 차단/통과 (반응 창)
CHALLENGE_BLOCK   차단 주장 → 나머지 의심/통과          (반응 창)
LOSE_CARD         특정 플레이어가 버릴 카드 선택         (해당 인 대기)
EXCHANGE_SELECT   대사 교환: 남길 카드 선택             (현재 턴 대기)
```

### 6.2 액션별 흐름

**소득**: 즉시 +1 → 다음 턴.

**해외원조**:
```
BLOCK_ACTION(공작 차단?) ─ 차단됨 → CHALLENGE_BLOCK
                        └ 통과 → +2 → 다음 턴
CHALLENGE_BLOCK: 차단 진짜 → 도전자 LOSE_CARD, 원조 무효
                차단 뻥  → 차단자 LOSE_CARD, +2
                통과     → 원조 무효
```

**쿠**: 코인 −7 → 대상 LOSE_CARD → 다음 턴. (반응 없음)

**세금**(공작):
```
CHALLENGE_ACTION ─ 의심: 진짜 → 도전자 LOSE_CARD +카드교체 → +3
                        뻥  → 액터 LOSE_CARD, 무효
                └ 통과 → +3 → 다음 턴
```

**암살**(암살자): 코인 −3(에스크로)
```
CHALLENGE_ACTION ─ 의심 뻥 → 액터 LOSE_CARD, 3환불, 무효
                 └ 진짜/통과 → BLOCK_ACTION(백작부인 차단?)
BLOCK_ACTION ─ 차단 → CHALLENGE_BLOCK(차단 진짜=대상 안전 / 뻥=대상 LOSE_CARD 후 암살도 진행→대상 2장 잃음)
             └ 통과 → 대상 LOSE_CARD(암살)
```

**강탈**(대장):
```
CHALLENGE_ACTION ─ 뻥 → 액터 LOSE_CARD, 무효
                 └ 진짜/통과 → BLOCK_ACTION(대장/대사 차단?)
BLOCK_ACTION ─ 차단 → CHALLENGE_BLOCK
             └ 통과 → 대상 코인 min(2,보유) 뺏음
```

**교환**(대사):
```
CHALLENGE_ACTION ─ 뻥 → 액터 LOSE_CARD, 무효
                 └ 진짜/통과 → EXCHANGE_SELECT(2장 뽑아 살아있는 카드 수만큼 유지, 나머지 덱 반환·셔플)
```

### 6.3 반응 창 규칙
- **의심 창**: 액터/차단자 제외 전원이 대상. **누군가 의심하면 즉시 처리**(1건만 유효), 전원 통과 또는 **제한시간 만료 시 자동 통과**.
- **차단 창**: 차단 자격자(대상, 해외원조는 전원)만. 차단 없으면 액션 진행.
- **LOSE_CARD**: 카드 1장이면 자동. 도전 해결 중 끼어드는 인터럽트 스텝(끝나면 원래 흐름 재개).

---

## 7. 봇 AI (혼자 플레이용)

- **자기 턴**: 코인 기준 — 10↑ 강제 쿠, 7↑ 자주 쿠(최강 상대 타깃), 그 외 가치 액션(세금/강탈/암살/교환/해외원조/소득)을 상황·보유 카드로 가중 선택. 정직봇은 보유 카드 위주, 블러핑봇은 가끔 없는 카드 주장.
- **반응**: 의심은 "그 카드가 이미 많이 노출/주장돼 확률 낮음"일 때 확률적으로. 차단은 타깃되면 보유(또는 블러핑) 차단 카드로.
- **LOSE_CARD**: 덜 유용하거나 중복 카드를 버림.
- **난이도**(추후): 초급=정직·소극 / 중급 / 고급=블러핑·영리한 의심.

---

## 8. 데이터 모델

### 8.1 인메모리 (RoomGame)
```
CoupGame
  phase                LOBBY/PLAYING/ENDED
  reactionSec          8/15/25
  players[] { seat, clientId, nick, isBot, coins, cards[ {char, revealed} x2 ], alive }
  deck[]               (character 리스트, 뒷면 더미)
  currentSeat
  pending { step, actorSeat, action, targetSeat, claimCard, blockerSeat, blockCard,
            responders(Set), passed(Set), loserSeat, deadline, refund }
  log[]                (공개 이벤트 로그)
  winnerSeat, version
```

### 8.2 응답 DTO — 정보 격리
- 내 카드: 캐릭터 **전부 공개**(나에게만).
- 남의 카드: **공개된(잃은) 카드만** 보이고, 숨은 카드는 개수만.
- 공개 정보: 코인, 현재 턴, pending(어떤 액션/주장/차단인지, 누구 응답 대기, 마감시각), 로그, 승자.
- 내가 응답 가능한지/무엇을 할 수 있는지 플래그(의심/차단/버릴카드/교환선택).

---

## 9. API (`/api/v1/coup`)
- `GET /rooms` · `POST /new`(reactionSec) · `POST /join` · `POST /add-bot`(level) · `POST /start`
- `POST /action` { type, targetSeat?, } — 내 턴 액션 선언
- `POST /respond` { decision: PASS|CHALLENGE|BLOCK, blockCard? } — 반응 창
- `POST /lose-card` { cardIndex } — 영향력 상실 선택
- `POST /exchange` { keepIndices } — 대사 교환
- `GET /me` · `POST /leave` · `POST /reset` · `POST /close-room`
- lazy tick: `me()`에서 반응 창 제한시간 처리 + 봇 진행.

---

## 10. 프론트 화면
- **보드**: 각 플레이어 카드(뒷면/공개), 코인, 생존/탈락, 현재 턴 강조.
- **내 손패**: 내 카드 2장 정체 공개(카드 일러스트/이모지).
- **액션바**(내 턴): 소득/해외원조/쿠/세금/암살/강탈/교환 + 타깃 선택.
- **반응바**(응답 가능 시): 의심 / 차단(카드) / 통과 + **카운트다운**.
- **모달**: 카드 잃기 선택, 교환 카드 선택.
- **로그 피드** + (있으면) 방 채팅.

---

## 11. 엣지 케이스
- 코인 10↑ → 쿠 강제(다른 액션 비활성).
- 강탈 시 대상 코인 0~1이면 있는 만큼만.
- 카드 교체(의심 성공 시) 후 덱 셔플. 교환 반환분도 셔플.
- 탈락자는 턴/반응에서 제외. 1인 남으면 종료.
- 반응 창 제한시간 만료 = 전원 통과.
- 나간 사람은 즉시 탈락 처리(루미큐브 패턴 참고) → 무한 대기 방지.

---

## 12. 권장 구현 순서
1. **1단계 — 뼈대**: 방/페이즈/세팅, 소득·해외원조·쿠, 카드 잃기·탈락·승리, 봇 기본, 정보 격리 DTO.
2. **2단계 — 캐릭터 액션 + 반응 창**: 세금/강탈/교환/암살 + 의심·차단·차단의심 상태머신 + 제한시간.
3. **3단계 — 봇 고도화**: 블러핑·영리한 의심, 난이도.
4. **4단계 — 다듬기**: 애니메이션·로그 연출·채팅.
5. (추후) 확장 카드(종교재판관 등) 변형.
