# 오목(五目 / Gomoku) 게임 설계서

> **게임명:** 오목
> **작성:** 2026-07-13
> **개발 브랜치:** `claude/spyfall-mafia-game-url-ks6y07`

---

## 1. 개요 · 확정 사항

바둑판 교차점에 흑·백을 번갈아 놓아 **먼저 5목(연속 5개)** 을 만들면 승리. 흑 선공.

| 항목 | 결정 |
|---|---|
| 판 크기 | **15×15** (표준 오목) |
| 룰 | **방 생성 시 선택** — 자유룰 / 금수룰(렌주) |
| 상대 | **봇(초·중·고급) 또는 다른 유저** (오델로와 동일 구조) |
| 선공 | 흑. 방장이 흑/백 선택(기본 흑) |

- 기존 인프라(방 + `clientId` + 인메모리 `RoomRegistry` + 폴링 + 봇) 재사용. 오델로 패턴을 그대로 따른다.

---

## 2. 규칙

### 2.1 공통
- 빈 교차점에 착수, 흑→백→흑… 번갈아.
- 가로·세로·두 대각선 중 하나로 **5개 연속**이면 즉시 승리.
- 빈칸이 없으면 무승부(거의 안 나옴).

### 2.2 자유룰 (기본·단순)
- 금수 없음. **5개 이상 연속(장목 포함)이면 승리.**
- 규칙이 가장 쉽고 캐주얼. 단, 흑(선공)이 다소 유리.

### 2.3 금수룰 (렌주 — 공정)
- **백은 제한 없음**(장목도 승리 인정).
- **흑 금수(둘 수 없음, 두면 반칙패 처리):**
  - **장목(6목 이상)** — 정확히 5만 승리로 인정.
  - **3-3** — 그 수로 "열린 3"이 동시에 2개 이상 생기면 금지.
  - **4-4** — 그 수로 "4"가 동시에 2개 이상 생기면 금지.
  - 단, **그 수로 정확히 5가 완성되면 승리**(금수보다 승리 우선).
- 판정: 착수점을 지나는 4방향(가로·세로·대각2) 라인의 패턴을 검사해 열린3/4/장목 개수를 센다.
- (구현 난이도: 3-3·4-4 판정이 렌주룰의 핵심 난제. 착수점 주변 패턴 매칭으로 처리.)

---

## 3. 봇 AI (초·중·고급)

착수 후보 = 기존 돌 주변 빈 교차점(탐색 범위 제한). 각 후보를 **패턴 점수**로 평가:
- 라인별 연속/열린 정도로 점수: 5(승리) ≫ 열린4 ≫ 4 ≫ 열린3 ≫ 3 ≫ 2 …
- 내 위협 생성 + 상대 위협 차단을 합산.

| 난이도 | 전략 |
|---|---|
| **초급** | 즉시 5/상대 4는 대응, 그 외엔 근처 무난한 수(1수 평가, 약하게) |
| **중급** | 전 후보 패턴 점수 평가로 최선(공·수 균형), 열린3 이상 위협 인지 |
| **고급** | 얕은 미니맥스/위협공간 탐색(2~4수)으로 **쌍삼·4-3 등 이중 위협**과 강제승 인지. 흑일 때 금수 회피 |

- 봇이 흑이고 금수룰이면 금수 자리는 후보에서 제외.

---

## 4. 데이터 모델

### 4.1 인메모리 (RoomGame, 오델로와 유사)
```
OmokGame
  phase          LOBBY/PLAYING/ENDED
  rule           FREE / RENJU
  hostColor      1(흑)/2(백)
  board[225]     0 빈칸 / 1 흑 / 2 백
  currentColor
  lastMove       (-1 없음)
  winner         0/1/2/3(무)
  winLine        승리 5칸(하이라이트용)
  players[] { seat, clientId, nick, ai, aiLevel, color }
  turnDeadlineMs, version, lastAction
```

### 4.2 응답 DTO
- board(225), currentColor, myColor, myTurn, lastMove, winner, winLine, rule, hostColor,
  forbidden(금수룰·내 차례일 때 흑 금수 칸 목록, UI 비활성화용), players, turn 남은시간, version.

---

## 5. API (`/api/v1/omok`) — 오델로와 동형
- `GET /rooms` · `POST /new`(hostColor, rule, turnSec?) · `POST /join` · `POST /add-bot`(level)
- `POST /start` · `POST /place`(cell) · `GET /me` · `POST /leave` · `POST /reset` · `POST /close-room`
- lazy tick: `me()`에서 봇 착수 + (있으면) 턴 타이머.

---

## 6. UI · 디자인 (여기 집중)

렌더는 **canvas** 권장(격자·돌 광택·승리 라인 연출에 유리, 기존 오델로/스네이크 패턴 재사용). 반응형으로 화면 폭에 맞춰 스케일.

### 6.1 바둑판
- **나뭇결 배경**(웜 우드 톤 그라디언트, 살짝 텍스처감).
- 15×15 **격자선**(가장자리는 굵게), **화점(★)** 5곳: (3,3)(3,11)(11,3)(11,11)(7,7).
- 가장자리 **좌표**(가로 A~O, 세로 1~15) — 옵션 토글.
- 여백/패딩으로 돌이 잘리지 않게.

### 6.2 돌
- **입체 바둑돌**: 방사형 그라디언트(흑=차콜→블랙 하이라이트, 백=화이트→라이트그레이) + 미세 그림자.
- **마지막 착수 표시**: 최근 돌 위에 작은 링/점.
- **놓기 미리보기**: 내 차례에 커서/탭 위치에 반투명 돌(호버, 데스크톱).

### 6.3 연출
- **승리 5목 하이라이트**: 5개 관통하는 반투명 라인 + 돌 글로우/펄스.
- 착수 시 살짝 튀는 애니메이션(놓이는 느낌).
- (금수룰) 흑 차례에 **금수 칸 X 표시**로 비활성화.
- 턴 인디케이터(●흑/○백 + 누구 차례), 승패 배너, 방 채팅.

### 6.4 반응형/입력
- 캔버스 = min(컨테이너 폭, 화면). 내부 해상도 고정 후 CSS 스케일 + devicePixelRatio 대응(선명).
- **탭/클릭 → 가장 가까운 교차점**에 착수. 오터치 방지(빈칸만).
- 라이트/다크 대응(보드는 우드톤 유지, 주변 UI만 테마).

---

## 7. 엣지 케이스
- 착수는 내 차례·빈칸·(금수룰) 금수 아님일 때만.
- 나간 사람 즉시 패배 처리 → 남은 1인 승리(무한 대기 방지).
- 봇이 둘 곳 없거나(판 참) 무승부 처리.
- 승리 판정은 착수점 기준 4방향만 검사(효율).

---

## 8. 구현 순서
1. **1단계 — 코어**: 판/착수/5목 승리(자유룰), 방·봇(초·중·고급), canvas 보드+돌 기본, 승리 라인.
2. **2단계 — 금수룰**: 3-3/4-4/장목 판정, 금수 칸 표시, 봇 금수 회피.
3. **3단계 — 디자인 마감**: 나뭇결·좌표·마지막수·호버 미리보기·승리 연출·애니메이션.
4. **4단계 — 봇 고도화**: 고급 위협공간 탐색 강화.
