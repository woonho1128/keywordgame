# 🐮 젝스님트 (6 nimmt! / Take 5) 설계 문서

## 0. 한눈에 요약

- **분류**: 방 기반 멀티플레이 카드게임(2~10인). 동시 선택 + 줄 배치 심리전.
- **구조**: 기존 방(RoomRegistry)·폴링·RoomChat 재사용. 마피아·쿠처럼 **서버가 상태(라운드/줄/점수)를 들고 턴을 관리**하는 방식.
- **봇**: 포함(혼자서도 플레이). 카드 선택 휴리스틱 봇 3난이도.
- **핵심 재미**: 모두 동시에 카드 1장을 엎어서 내고 → 낮은 숫자부터 줄에 놓임 → **6번째 카드를 놓는 사람이 그 줄 5장을 벌점으로 가져감.** 벌점(황소 머리)을 적게 먹는 게 목표.

---

## 1. 규칙

### 1.1 카드 · 벌점(황소 머리)
- 카드: **1~104** 숫자 카드 104장.
- 각 카드의 벌점(황소 머리 수):
  | 조건 | 벌점 |
  |---|---|
  | 55 | 7 |
  | 11의 배수(11·22·…·99) | 5 |
  | 10의 배수(10·20·…·100) | 3 |
  | 5의 배수(끝자리 5) | 2 |
  | 그 외 | 1 |

  (계산 순서대로 적용: 55 → 11배수 → 10배수 → 5배수 → 1)

### 1.2 준비
- 각 플레이어에게 **10장** 배분.
- 남은 덱에서 **4장**을 뽑아 **4개 줄**의 시작 카드로 1장씩 놓음.

### 1.3 한 라운드(트릭) 진행 — 총 10라운드(손패 10장)
1. **동시 선택**: 모든 플레이어가 손패에서 1장을 **엎어서** 냄.
2. **공개 & 배치**: 낸 카드를 **숫자 낮은 순서대로** 하나씩 배치.
   - 각 카드는 **"자기보다 작으면서 가장 가까운" 줄 끝 카드** 뒤에 놓임(오름차순 유지).
   - 그 카드가 줄의 **6번째**가 되면 → 그 줄의 **앞 5장을 벌점으로 가져가고**, 낸 카드가 그 줄의 새 시작이 됨.
   - 낸 카드가 **모든 줄 끝보다 작으면** → 그 사람이 **줄 하나를 선택해 통째로 가져가고**(보통 벌점 적은 줄), 낸 카드가 그 줄의 새 시작이 됨.
3. 10라운드 반복 → 손패 소진 → **한 판(hand) 종료**.

### 1.4 게임 종료 · 승리
- 한 판이 끝나면 벌점을 누적하고 **다시 배분해 새 판** 시작.
- **누군가 누적 벌점 66점 이상**이 되면 게임 종료 → **총 벌점 최소인 사람이 승리.**
- (대안: "N판 고정" 모드 — 아래 결정사항)

---

## 2. 상태 머신 (서버)

```
LOBBY
  └ start →
PLAYING 반복:
  SELECT   : 전원 카드 1장 선택(엎기). 타이머. 봇 자동 선택.
     └ 전원 제출 → RESOLVE
  RESOLVE  : 낸 카드를 낮은 순으로 하나씩 배치.
     ├ 6번째 → 그 줄 회수(벌점), 카드가 새 시작
     ├ 전부보다 작음 → CHOOSE_ROW (그 플레이어가 가져갈 줄 선택) → 계속
     └ 모두 배치 끝 →
        ├ 손패 남음 → 다음 SELECT
        └ 손패 0 → 판 정산 → 66점 넘김? → ENDED / 아니면 새 판 배분 후 SELECT
CHOOSE_ROW : 특정 플레이어의 줄 선택 대기(나머지는 대기). 타이머 초과 시 벌점 최소 줄 자동 선택.
ENDED
```

- **동시성 처리**: SELECT에서 각자 낸 카드는 서버에 숨겨 저장(다른 사람에게 안 보임). 전원 제출(또는 타이머) 시 RESOLVE로 전환하며 일괄 공개.
- **폴링**: 클라 `/me` 1초 폴링으로 현재 페이즈·줄 상태·내 손패·점수·연출 정보 수신. 봇 행동은 `tick()`에서 지연 처리(기존 패턴).

### 2.1 엔드포인트 (기존 규약)
| 메서드 | 경로 | 용도 |
|---|---|---|
| GET | `/rooms` | 방 목록 |
| POST | `/new` | 방 생성 `{nick}` |
| POST | `/join` | 참가 |
| POST | `/add-bot` | 봇 추가(방장, 난이도) |
| POST | `/start` | 시작(방장) |
| POST | `/play` | 카드 내기 `{card}` (SELECT) |
| POST | `/take-row` | 줄 선택 `{row}` (CHOOSE_ROW) |
| GET | `/me` | 상태 조회 |
| POST | `/leave` | 나가기 |

---

## 3. 연출 (핵심 — 동시 공개의 쾌감)

- **동시 공개**: SELECT 끝나면 모두의 카드가 한 번에 뒤집히는 연출.
- **순차 배치**: 낮은 카드부터 줄에 **슥 미끄러져 들어가는** 애니메이션(누가 어디 놓는지 보임).
- **6번째 회수**: 줄이 꽉 차 벌점으로 쓸려가는 순간 강조(빨강 번쩍 + "쾅").
- **황소 머리**: 카드마다 벌점(황소 아이콘/숫자) 표시 → 위험한 카드 직관적으로.
- 모바일: 4줄을 세로로 쌓아 보여주고, 손패는 하단 가로 스크롤 + 탭으로 선택.

---

## 4. 봇 AI (휴리스틱 3난이도)

카드 선택 시 각 손패 카드의 "위험도"를 평가:
- 어떤 줄 끝 바로 위에 딱 붙는 카드(적게 벌어짐) → 안전.
- 그 줄이 이미 5장(내가 6번째로 회수 위험) → 위험 회피.
- 모든 줄보다 낮은 카드(줄 회수 강제) → 벌점 적은 줄이 있을 때만.
- 높은 카드는 나중을 위해 아껴두거나, 안전할 때 소비.

| 난이도 | 특징 |
|---|---|
| 초급 | 6번째만 대충 피함, 랜덤성 큼 |
| 중급 | 위험도 상위 회피 + 낮은카드 관리 |
| 고급 | 줄 상태·상대 카드 분포 고려, 회수 최소화 최적수 |

---

## 5. 기술 구조

### 5.1 백엔드 (신규 `com.wordplay.sixnimmt`)
- `SixNimmtGame implements RoomGame` — 방·상태머신·정산.
- `SixNimmtBot` — 선택 휴리스틱.
- `SixNimmtRoomManager` / `SixNimmtController`.
- 상태 응답 DTO: 페이즈, 4줄(각 카드+벌점), 내 손패, 각자 제출여부(카드값은 숨김), 점수, 라운드/판, 회수 로그(연출용), 승자.

### 5.2 프론트 (`/sixnimmt`)
- 로비(생성·코드참가·방목록·봇추가·시작) → 게임(4줄·손패·선택·정산·점수판) → 결과.
- RoomChat·로비챗 재사용.

### 5.3 홈
- 게임 목록에 🐮 젝스님트 카드 추가(봇 포함 → `solo:true`).

---

## 6. 결정 필요 사항

1. **게임 종료 방식**: (a) **66점 도달 시 종료**(클래식·추천) vs (b) **N판 고정**(짧고 예측가능).
2. **최대 인원**: 표준 10인 vs 가독성 위해 8인 제한.
3. **봇**: 포함(추천, 혼자 가능) vs 사람만.
4. (선택) **프로 변형**: 덱을 `1~(10×인원+4)`로 줄여 빈틈 없는 하드 모드 — v1 생략, 2차 옵션.

---

## 7. 마일스톤

1. **엔진 코어**: 덱·벌점·배치 규칙(6번째 회수/줄 회수)·정산·66점 종료 — 순수 로직 + 단위 테스트.
2. **상태 머신**: SELECT→RESOLVE→CHOOSE_ROW 전환 + 동시 제출 + 타이머 + 봇 tick.
3. **백엔드 방/컨트롤러** + RoomRegistry.
4. **프론트**: 로비 + 4줄/손패 UI + 선택/정산 폴링.
5. **연출**: 동시 공개·순차 배치·회수 강조 + 모바일 레이아웃.
6. **봇 3난이도** + 홈 등록 + 채팅.
