# 경마 게임 설계서

> **프로젝트:** gg 파티게임 플랫폼 내 신규 게임
> **게임명:** (가칭) 경마 (Horse Race)
> **작성:** 2026-07-13
> **개발 브랜치:** `claude/spyfall-mafia-game-url-ks6y07`

---

## 1. 개요

배팅의 긴장감이 핵심인 경마 게임. 기존 파티게임들과 달리 **"잔고가 쌓이는 맛"** 이 게임의 본질이므로, 이 게임에 한해 **선택형 영속 계정(닉+암호)** 을 도입한다.

- **레이스 자체보다 배팅이 게임의 중심**이다. 레이스는 가중 랜덤이고, 실력은 배당을 읽고 뱅크롤을 관리하는 데 있다.
- 기존 인프라(방 + `clientId` + 인메모리 `RoomRegistry` + 폴링 + lazy-tick + 봇)를 최대한 재사용한다.
- **놀이용 가상 칩이며 실제 환전은 없다.** 모든 화면에 명시한다.

### 1.1 핵심 결정 요약

| 항목 | 결정 |
|---|---|
| 계정 | **처음부터 도입.** 닉+암호 영속 계정 + 게스트(세션 칩) 하이브리드 |
| 돈 처리 | 로그인 사용자 = 본인 영속 잔고 / 게스트 = 방-한정 세션 칩 |
| 경마 종류 | **기본경마** + **특수경마(유머)** 2종 |
| 배당 방식 | **고정 배당 · 펀드풀** 2모드 (방 생성 시 선택) |
| 배팅 권종 | 단승 · 연승 · 쌍승 · 삼복승 |
| 재기 보너스 | 파산 시 지급, **1일 횟수 제한** |

---

## 2. 계정 & 지갑 시스템

### 2.1 진입 흐름

경마 진입 시 두 갈래를 제공한다.

```
경마 첫 화면
 ├─ 게스트로 플레이   → 닉네임만. 방-한정 세션 칩
 └─ 로그인 / 가입     → 닉네임 + 암호. 영속 잔고
       ├─ 신규 닉네임 → 가입(시작 잔고 지급)
       └─ 기존 닉네임 → 암호 검증 후 로그인
```

- 한 테이블에 **게스트와 계정 플레이어가 섞여도 무방**하다. 배팅·정산 로직은 동일하며, **계정 플레이어만 레이스 정산 결과가 서버 잔고에 반영**된다.
- 로그인 성공 시 서버가 **세션 토큰**을 발급하고 프론트는 localStorage(`horserace_token`)에 저장한다. 재접속 시 토큰으로 자동 로그인.

### 2.2 지갑 규칙

| 구분 | 시작 칩 | 지속성 | 정산 반영 |
|---|---|---|---|
| 게스트 | 방 설정 buy-in (기본 5,000) | 방 한정, 나가면 소멸 | 테이블 스택만 변동 |
| 계정 | 가입 시 10,000 (이후 본인 잔고) | 서버 영속 | 매 레이스 결과가 잔고에 즉시 반영 |

- **계정 플레이어는 자기 잔고를 그대로 들고 테이블에 앉는다.** 배팅 결과가 잔고에 바로 반영된다(도박 특유의 긴장감 유지). 뱅크롤 보호는 재기 보너스로 한다.
- 게스트는 방 안에서만 통용되는 세션 칩을 쓴다. 목적은 그 방의 리더보드 1등.

### 2.3 재기 보너스 (일일 제한)

- 잔고가 **임계값 이하(기본: 100 미만)** 이면 `재기 보너스` 버튼 활성화 → 소액(기본 2,000) 지급.
- **1일 횟수 제한(기본 3회)**. 서버 날짜 기준.
  - `last_bonus_date != 오늘` 이면 `bonus_count_today = 0` 으로 리셋 후 지급.
  - `bonus_count_today >= 3` 이면 다음 날까지 대기.
- 게스트는 방 설정에 따라 무제한 리바이(또는 방장 재량)로 별도 처리.

### 2.4 보안 (놀이머니 수준으로 필요한 만큼)

- **비밀번호 BCrypt 해시.** 평문 저장·로깅 절대 금지.
- 로그인 실패 N회(기본 5회) 시 짧은 잠금(기본 60초) — brute force 방지.
- 최소 암호 길이(기본 4자, 놀이머니라 느슨).
- 요청 rate limit(기존 `clientId` 검증 재사용).
- **암호 분실 = 계정 분실**(이메일 복구 없음). 놀이머니라 수용.
- 화면 고지: "가상 칩 · 실제 환전 아님 · 암호 분실 시 복구 불가".

---

## 3. 게임 흐름

방 기반 + 폴링. 페이즈:

```
LOBBY → [ BETTING → RACING → RESULT ] 무한 반복 → (방장 종료) ENDED
```

| 페이즈 | 내용 |
|---|---|
| **LOBBY** | 참가·봇 추가, 방 설정 확인. 방장이 시작 |
| **BETTING** | 제한시간(기본 25초). 말 목록·컨디션·각질·폼·배당 표시. 각자 권종별 칩 배팅. 남은 시간·판돈 실시간 |
| **RACING** | 레이스 재생(§7). 전체 결과는 시작 시 확정, 연출만 재생. 모두 같은 화면 |
| **RESULT** | 착순·배당·각자 손익 정산 → 칩/잔고 갱신. 이월/탈락 표시. 방장이 "다음 레이스" |
| **ENDED** | 방장이 종료 시. 최종 칩 리더보드 |

- **세션 종료: 무한 진행 + 방장이 종료**(친구끼리 계속 도는 느낌). 이월 로스터는 방이 살아있는 동안 유지, 종료 시 소멸. (옵션: 방 설정에 "N판 후 자동 종료" 추가 가능)
- 방 설정(생성 시): 경마 종류(기본/특수), 배당 모드(고정/펀드풀), buy-in, 배팅 제한시간, 말 마릿수(기본 9), (선택)자동종료 판수.

---

## 4. 경마 종류

### 4.1 기본경마 (Basic)

- 정통 경마. 말은 **앞으로만** 전진.
- 매 tick 전진량 = `기본속도 × 컨디션 + 랜덤노이즈`.
- 능력치가 결과에 정직하게 반영 → 인기마/언더독 배당이 실력과 일관.

### 4.2 특수경마 (Special, 유머)

- 이벤트/장애물로 예측불가·웃김. 능력치 영향이 줄고 랜덤성이 커져 **배당이 평평**(언더독도 잘 이김) → 한탕 노리는 재미.
- 매 tick 각 말에 낮은 확률로 랜덤 이벤트 발동:

| 이벤트 | 효과 |
|---|---|
| 🔙 뒷걸음질 | 잠깐 뒤로 감 |
| 🍌 바나나 | 미끄러져 정지 + 순위 뒤바뀜 |
| 🚀 부스터 | 갑자기 폭주 |
| 😴 낮잠 | 몇 tick 멈춤 |
| 🌀 워프 | 앞/뒤로 순간이동 |
| 🐢 역전 버프 | 꼴찌 말에게 일시 가속 |

- **배당 계산도 이벤트를 포함한 시뮬레이션**으로 뽑아야 정확하다(§6).
- 결과 화면에 "무슨 일이 있었는지" 로그를 남겨 웃음 포인트로.

---

## 5. 말(Horse) 데이터 — 세션 순환 로스터

**매 레이스 9마리 출전(기본).** 고정된 "전체 말 풀"은 없다. 대신 **직전 레이스 상위 입상마가 살아남아** 연승 이력을 쌓고, 나머지 자리는 신입 말로 채운다 → 세션(방) 안에서만 유효한 순환 로스터.

### 5.1 출전 · 순환 규칙

- 출전 두수: **기본 9마리**(방 설정 가능).
- **직전 1~3등 = 재출전(이월)**, 나머지 6자리 = **신입 말**(fresh 랜덤).
- **첫 레이스는 전원 신입.**
- 재출전 말이 이번에 **4등 이하로 밀리면 탈락**(다음 판 신입으로 교체) → 로스터가 계속 물갈이된다.
- 결과적으로 "잘 나가던 말"이 한 번 삐끗하면 사라져 자연 순환.

### 5.2 말이 가진 것

| 필드 | 공개? | 역할 |
|---|---|---|
| `name` / `emoji` | 공개 | 이름·이모지(🐎🦄🐴 등) |
| `condition` ★1~5 | **공개** | **강함 dial**(속도 베이스). 매판 재롤. 인기마/언더독을 만드는 실력 축 |
| `runStyle` 각질 | **공개** | 🏃선행(초반 빠름·후반 페이드) / 🐆추입(초반 느림·막판 폭발) / ⚖️평준. 매판 재롤 |
| `formLine` 폼 라인 | **공개** | 세션 내 최근 착순(예 `1-2-1`). 재출전 말만. 신입은 "🆕신입" |
| `streak` 연속 입상 | **공개(🔥 뱃지)** | 연속 top3(또는 연승) 카운트. 배당에 반영 |

- **강함 축은 컨디션 ★ 하나로 단일화**. ★는 1~5로 거칠게 줘서 같은 ★4가 여럿 나오고, 그 안에서는 각질·폼·랜덤이 승부를 가른다.
- **컨디션·각질은 매 레이스 재롤**되지만, **폼 라인·스트릭은 세션 내에서 누적**(재출전 말 한정). 그래서 연승 이력이 진짜 의미를 갖는다 — 실제로 그 방에서 그렇게 달렸으니까.

### 5.3 연승 → 배당 반영

- **재출전·연승마는 폼 보너스**로 실제 우승확률이 소폭 상승 → 배당이 낮게 붙는다(인기마화). 몬테카를로 시뮬(§6.1)이 이 보너스를 포함해 배당을 산출.
- **눈덩이 방지 2중 장치**:
  1. 폼 보너스에 **캡**(예 스트릭 3 이상은 동일) → 무한 강화 차단.
  2. **컨디션 매판 재롤** → 3연승 중이어도 폼 안 뜨는 날 4등으로 밀려 **탈락 가능** → 스트릭이 자연히 끊긴다.
- 결과: "🔥3연승, 배당 2.0배지만 믿고 간다" vs "🆕신입인데 8배네 한탕?" 같은 읽는 재미.

### 5.4 비결정성 튜닝 (핵심)

- ★·폼으로 속도 베이스를 정하되, **매 tick 노이즈를 그 차이 대비 충분히 크게** 잡는다. 30~40틱 누적되면 최고 인기마도 우승률이 **30~40%대**(절반 이상 패배)에 수렴 → 언더독 대박.
- 이 노이즈 크기가 핵심 튜닝 상수("실력차 반영도"). 크게=혼돈/언더독↑, 작게=예측/실력↑.
- 검증: 매판 로스터로 수천 회 시뮬(§6.1) → 특정 말에 우승이 과도하게 쏠리지 않는지, 스트릭이 영구 지배로 이어지지 않는지 확인.

---

## 6. 배당(Odds) 시스템

### 6.1 우승·착순 확률 산출 — 몬테카를로

- 배당(특히 쌍승/삼복승)은 스탯만으로 닫힌 식이 어렵다. **레이스를 수천 회 시뮬(경마 종류·이벤트 포함)** 해 각 착순 통계를 뽑는다.
  - 단승 확률 `p_win[i]`, 연승 확률 `p_place[i]`, 쌍승 확률 `p_exacta[i,j]`, 삼복승 확률 `p_trio[set]`.
- 시뮬은 배팅 페이즈 진입 시 1회 수행(방 tick 밖, 결정적 시드는 아님). 결과를 캐시.

### 6.2 두 배당 모드 (방 생성 시 택1)

**고정 배당 (Fixed)**
- 배당률 = `(1 / p) × (1 − 하우스마진)`, 마진 기본 0.2.
- 언더독 자연히 고배당. **인원 무관 안정적** → v1 기본.
- 배팅 시점에 배당 확정, 하우스가 지급.

**펀드풀 (Pari-mutuel)**
- 권종별 풀을 모아 적중자끼리 분배: `배당 = 풀총액 × (1−마진) / 적중자_배팅합`.
- 배당이 실시간 변동(눈치싸움). 배팅 마감 시 확정.
- **소인원 보정**: 각 말/조합에 `p` 비례 **하우스 시드 판돈**을 미리 주입해 배당이 이상해지지 않게 한다. 시드는 하우스 몫(적중자에게 환급 안 함).

### 6.3 배팅 권종

착순 배열로 정산.

| 권종 | 조건 | 성격 |
|---|---|---|
| 단승 WIN | 1등 적중 | 기본 |
| 연승 PLACE | 3등 이내(마릿수 적으면 2등 이내) | 안전·저배당 |
| 쌍승 EXACTA | 1·2등 **순서까지** 적중 | 고배당 |
| 삼복승 TRIO | 1·2·3등(**순서 무관**) 적중 | 초고배당 |

- (추후) 삼쌍승(순서유관), 복승(1·2등 순서무관) 등 확장 가능.

---

## 7. 레이스 진행 — "선(先)계산 · 동기 재생"

> 핵심 요구: **모든 참가자가 정확히 같은 화면을 본다.** 폴링(1초)으로는 300ms 틱을 못 따라가 뚝뚝 끊기므로, 서버가 레이스를 통째로 미리 계산해 **타임라인**을 내려주고, 클라는 **서버 시계 기준**으로 로컬 재생한다.

### 7.1 서버: 레이스 통째 계산 (RACING 진입 순간)

- 트랙 내부 길이 `L`. 내부적으로 `T`틱(기본 ~36틱) 시뮬레이션.
- 매 틱 전진량:
  ```
  pos[i] += base(condition_i, formBonus_i) × paceCurve(runStyle_i, 진행률) + noise
  noise = 랜덤 × 기준분산     // ★·폼 차이 대비 충분히 크게(§5.4)
  ```
  - `base(condition, formBonus)`: ★1~5 + 연승 폼 보너스(캡 적용)를 속도 베이스로 매핑.
  - `paceCurve`: 선행마 초반↑·후반↓(페이드), 추입마 반대(막판 폭발), 평준 평탄.
  - **noise가 ★·폼 차이 대비 충분히 커서** 최고 인기마도 자주 진다(§5.4).
  - 특수경마: 위 + 이벤트 롤(§4.2)로 pos 가감 + **이벤트 로그**(무슨 일이 언제 났는지) 기록.
- 산출물: **각 틱의 전 말 위치 배열(타임라인)** + **착순** + **이벤트 로그** + `raceStartAt`(서버 타임스탬프) + `tickMs`.
- **서버 권위**: 결과는 이 순간 확정(클라 조작 불가). 이후 tick 루프 불필요 — 타임라인만 응답에 실린다.

### 7.2 클라: 동기 재생

- 기존 게임들처럼 `serverNow` - `Date.now()` 로 **시계 오프셋**을 구해, 현재 재생 프레임 = `(보정된 현재시각 − raceStartAt) / tickMs` 로 계산 → **누가 언제 접속·폴링하든 같은 순간 같은 위치**를 그린다(늦게 들어와도 그 시점부터 이어 재생).
- 프레임 사이는 위치 **보간**으로 부드럽게(300ms 틱 → 60fps 보간).
- 재생이 타임라인 끝(착순 확정)에 도달하면 RESULT로.
- 근소차면 **포토피니시**(막판 슬로모 + 확대)로 결과 공개를 살짝 지연.

### 7.3 연출 · 그래픽 설계 (신경 써서)

- **트랙**: 잔디/모래 질감, 9개 레인, 출발 게이트, 결승선, 거리 마커. 경마장 느낌.
- **말**: 레인별 색·번호, 달리는 모션(상하 바운스), 먼지·스피드라인, 선두 강조. (렌더는 **canvas** 권장 — 9마리 부드러운 모션·이펙트·포토피니시에 유리. 기존 snake가 canvas 사용 중이라 패턴 재사용)
- **배당판**: 전광판 스타일 배당·컨디션 ★·각질·🔥연승 표시.
- **레이스 중**: 실시간 순위, 특수경마 이벤트 팝(🍌🚀😴) 애니메이션.
- **포토피니시**: 결승선 클로즈업 + 슬로모.
- **결과**: 착순 시상대, 배당 점등, 내 손익 카운트업, 이월/탈락 화살표, (특수경마) 이벤트 리플레이 로그.
- 라이트/다크 대응, 모바일 세로 우선 레이아웃.

---

## 8. 정산 로직

RESULT 진입 시:

1. 착순 배열 확정.
2. 각 배팅에 대해 권종별 적중 판정.
3. 배당 모드에 따라 지급액 계산(고정: 배당률×배팅 / 펀드풀: 풀 분배).
4. 플레이어별 손익 반영:
   - 게스트 → 테이블 세션 칩 갱신.
   - 계정 → **DB 잔고 즉시 갱신**, `peak_balance`·`total_races`·`wins` 갱신.
5. 파산(잔고<임계) 계정에 재기 보너스 버튼 활성.
6. 방장이 다음 레이스 or 종료.

---

## 9. 봇 (배팅 봇)

- 레이스는 말이 뛰므로 봇은 **"배팅자"** 로만 존재(혼자 플레이·펀드풀 풀 채우기용).
- 성향 랜덤: 인기마 선호형 / 언더독 도전형 / 분산형.
- 배당·컨디션을 보고 확률적으로 권종·금액 결정. 세션 칩만 사용(잔고 영속 없음).
- 난이도는 필요 시 추가(정보 반영 정도 차이).

---

## 10. 데이터 모델

### 10.1 영속 (DB, Postgres)

```
race_account
  id                BIGSERIAL PK
  nickname          VARCHAR UNIQUE       -- 계정 키
  password_hash     VARCHAR              -- BCrypt
  balance           BIGINT               -- 가상 칩
  peak_balance      BIGINT
  total_races       INT
  wins              INT
  last_login_at     TIMESTAMP
  last_bonus_date   DATE                 -- 재기 보너스 일일 제한
  bonus_count_today INT
  created_at        TIMESTAMP
```

- 세션 토큰: 인메모리 `Map<token, accountId>`(재시작 시 재로그인 필요, 놀이머니라 수용) 또는 DB 테이블. v1은 인메모리.

### 10.2 인메모리 (방 상태, `RoomGame` 구현)

```
HorseRaceGame
  phase                LOBBY/BETTING/RACING/RESULT/ENDED
  raceType             BASIC/SPECIAL
  oddsMode             FIXED/PARIMUTUEL
  buyIn, betSec, horseCount, autoEndRounds?, round
  players[]  { seat, clientId, nick, isBot, isAccount, accountId, tableChips, leftFlag }
  horses[]   { id, name, emoji, condition(★1~5), runStyle, formLine[], streak, isNew }
                                                          // 이월 3 + 신입 6, 매판 재구성
  odds       { pWin[], pPlace[], exacta{}, trio{}, 배당률 캐시 }
  bets[]     { seat, type(WIN/PLACE/EXACTA/TRIO), pick(말 or 조합), amount }
  race       { timeline[T][N], finishOrder[], eventLog[], raceStartAt, tickMs }  // 선계산·동기재생(§7)
  betDeadlineMs, version
```

- 방 자체는 인메모리(기존과 동일). **계정 잔고만 DB 영속.**
- 계정 플레이어의 `tableChips`는 사실상 잔고의 뷰 — 정산 시 DB에 반영.

### 10.3 응답 DTO (요지)

`HorseRaceStateResponse` — phase, raceType, oddsMode, 내 지갑(칩/잔고·계정여부), 말 목록(컨디션·배당), 내 배팅, 판돈 현황(펀드풀), 배팅 남은시간, 레이스 위치, 착순, 이벤트 로그, 플레이어 리더보드, version 등. 정보 격리(남의 배팅 상세는 필요 범위만).

---

## 11. API 엔드포인트 (`/api/v1/horserace`)

**계정**
- `POST /account/auth` (nickname, password) → 로그인 or 가입, {token, balance}
- `GET  /account/me?token=` → 잔고·전적
- `POST /account/bonus?token=` → 재기 보너스(일일 제한 검사)
- `GET  /leaderboard` → 부자 랭킹(계정 balance 상위)

**방/게임** (기존 게임들과 동형)
- `GET  /rooms`
- `POST /new?clientId=` (방 설정) → 방 코드
- `POST /join?roomCode=&clientId=` (게스트 nick 또는 token)
- `POST /add-bot?roomCode=&clientId=`
- `POST /start`
- `POST /bet?roomCode=&clientId=` (type, pick, amount) — BETTING 페이즈만
- `POST /next-race`
- `GET  /me?roomCode=&clientId=`
- `POST /leave` · `POST /reset` · `POST /close-room`(관리자 코드)

---

## 12. 프론트 화면 흐름

1. **진입**: 게스트 vs 로그인/가입 선택. 고지 문구.
2. **로비**: 방 목록 / 방 생성(경마종류·배당모드·buy-in·제한시간·마릿수·(선택)자동종료판수). 부자 랭킹 보기.
3. **대기방**: 참가자·봇, 방장 시작.
4. **배팅**: 말 목록(이모지·컨디션 ★·각질·🔥연승·배당), 권종 탭(단/연/쌍/삼복), 칩 걸기, 남은시간·풀 현황, 내 지갑.
5. **레이스**: canvas 트랙 애니메이션(§7.3), 서버 시계 동기 재생, (특수경마) 이벤트 팝.
6. **결과**: 착순·포토피니시, 내 손익, 칩/잔고 갱신, 이월/탈락, 이벤트 로그, 다음 레이스/파산 재기 버튼.
7. **랭킹**: 글로벌 부자 랭킹.

- 홈 카드 추가, 전역 홈 버튼(기존 `HomeButton`) 그대로 적용.

---

## 13. 공정성 / 무결성

- 레이스 결과·타임라인은 **서버에서만 계산**(§7). 클라 입력은 배팅뿐.
- **동기 재생**: 모든 클라가 `raceStartAt` + 서버 시계 오프셋으로 동일 프레임을 그린다 → 모두 같은 화면.
- 배팅은 BETTING 페이즈에만 허용, 마감 후 변경 불가.
- 배당 확정 시점: 고정=배팅 시점 / 펀드풀=배팅 마감 시점.
- **계정 = 동시 1개 방에서만 플레이**: 로그인 계정이 다른 방에 진입 시 "이미 다른 방에서 플레이 중" 차단(같은 잔고 동시 배팅 방지). 방 나가면 해제.
- 계정 잔고 변경은 서버 정산에서만 **원자적**으로. 음수 방지, 배팅액 ≤ 보유 칩 검증.
- 배팅 단위 **10칩**, 1회 최소 10·최대 보유량, 여러 말/권종 분산 배팅 허용.
- 엣지: 무배팅이어도 레이스 진행(로스터 갱신). 파산+보너스 소진 계정은 관전. 공동 착순은 미세 랜덤 타이브레이크.
- 암호 해시·로그인 잠금·rate limit.

---

## 14. 권장 구현 순서 (목표는 전부 포함)

> 사용자 요구: 계정·2종류 경마·2배당모드·고급 배팅 모두 포함. 아래는 안전한 **구현 순서**이지 범위 축소가 아니다.

1. **1단계 — 뼈대 + 계정**: DB `race_account`, 계정 auth/토큰/보너스, 방·페이즈·기본경마·고정배당·단승/연승·봇·정산·리더보드.
2. **2단계 — 고급 배팅**: 쌍승/삼복승 + 몬테카를로 배당 확장.
3. **3단계 — 펀드풀 모드**: 권종별 풀·하우스 시드·실시간 배당.
4. **4단계 — 특수경마**: 이벤트 엔진 + 이벤트 포함 시뮬 + 결과 로그 연출.
5. **다듬기**: 부자 랭킹 UI, 파산 재기 UX, 애니메이션·포토피니시.

---

## 15. 확정된 기본값 (구현 기준)

- **레이스 연출**: 선계산 타임라인 + 서버 시계 동기 재생, 모두 같은 화면, canvas 그래픽(§7).
- **세션 종료**: 무한 진행 + 방장 종료(옵션 자동종료 판수).
- **계정 무결성**: 동시 1개 방만, 원자적 잔고 갱신.
- **buy-in**: 게스트 세션 칩에만 적용. 계정은 자기 잔고 그대로.
- **배팅**: 10칩 단위, 최소 10·최대 보유량, 분산 배팅 허용.
- **재출전**: 직전 top3 이월, 4등↓ 탈락.

## 16. 남은 튜닝/논의 (나중에)

- **하우스 마진 값**, 재기 보너스 금액·임계·일일 횟수.
- **연승 폼 보너스 크기·캡**, 노이즈 크기(실력차 반영도).
- **특수경마 이벤트 확률·종류** 밸런싱.
- **게스트 리바이 정책**(무제한/방장 재량).
- **일일 접속 보너스**, 업적/칭호 등 리텐션 요소.
- 토큰 영속(서버 재시작 시 유지) 필요 여부.
