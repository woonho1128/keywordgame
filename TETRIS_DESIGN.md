# 🧱 테트리스 (Tetris) 설계 문서

## 0. 한눈에 요약

- **분류**: 실시간 1인 아케이드 게임 (snake / territory 와 동일 계열)
- **게임 루프**: **100% 프론트엔드**(브라우저)에서 실행. 서버는 게임 진행에 관여하지 않음.
- **백엔드 역할**: **랭킹 저장/조회만** — 기존 `tb_score` 공용 랭킹 시스템 재사용 (`snake`, `territory` 와 동일 방식).
- **표준**: 현대 테트리스 가이드라인(Tetris Guideline)을 따름 — 7-bag, SRS 회전, 홀드, 고스트, 넥스트 큐, 하드드롭.
- **디자인**: 모바일/PC 둘 다 1급 대응. 네온/글래스 스타일로 시각 완성도 신경.

왜 이 구조냐:
- 마피아·쿠·오목처럼 서버가 매 턴 상태를 들고 폴링으로 동기화하는 방식은 테트리스에 안 맞음. 테트리스는 **초당 수십 프레임으로 블록이 떨어지는 실시간 게임**이라 1초 폴링으로는 절대 못 굴림.
- 대신 snake처럼 **브라우저가 게임을 다 굴리고, 죽으면 최종 점수만 서버에 제출**하면 됨. 서버 부하도 거의 없고 구현도 단순.

---

## 1. 게임 규칙 (Tetris Guideline 준수)

### 1.1 보드
- **10칸(가로) × 20칸(세로)** 표준 플레이필드.
- 상단에 스폰용 여유 2줄(버퍼 존) — 실제로는 20줄만 보이게.

### 1.2 블록(테트로미노) 7종
표준 색상(가이드라인):

| 블록 | 모양 | 색 |
|------|------|-----|
| I | 일자 4칸 | 하늘색(cyan) |
| O | 정사각 | 노랑(yellow) |
| T | T자 | 보라(purple) |
| S | S자 | 초록(green) |
| Z | Z자 | 빨강(red) |
| J | J자 | 파랑(blue) |
| L | L자 | 주황(orange) |

### 1.3 랜덤: 7-bag 시스템
- 7종을 한 봉지에 담고 섞어서 하나씩 꺼냄 → 다 꺼내면 새 봉지.
- **"같은 블록만 계속 나오거나, 특정 블록이 20번 안 나오는 불운"이 원천 차단됨.** 현대 테트리스 표준.

### 1.4 회전: SRS (Super Rotation System)
- 시계/반시계 회전.
- **월킥(Wall Kick)**: 벽이나 다른 블록에 막혀 못 도는 자리에서, 정해진 오프셋(SRS 킥 테이블)으로 살짝 밀어서 회전 성사 → T-스핀 등 고급 기술의 근간.
- I 블록과 나머지(J/L/S/T/Z)는 킥 테이블이 다름. O는 회전해도 그대로.

### 1.5 조작
| 동작 | 설명 |
|------|------|
| 좌/우 이동 | 한 칸씩. 길게 누르면 연속 이동(DAS/ARR) |
| 소프트 드롭 | 아래로 빠르게 하강(점수 소량) |
| 하드 드롭 | 즉시 바닥에 꽂고 고정(점수 보너스) |
| 회전 CW/CCW | 시계/반시계 |
| 홀드(Hold) | 현재 블록을 보관함에 넣고 교체. **한 블록당 1회**(고정 전까지 다시 홀드 불가) |

### 1.6 락 딜레이 (Lock Delay)
- 블록이 바닥에 닿아도 **즉시 안 굳고 약 0.5초 여유** → 그 사이 좌우 이동/회전으로 마지막 조정 가능.
- 이동/회전할 때마다 딜레이 리셋(무한 리셋 방지용 횟수 상한 15회).

### 1.7 고스트 피스 (Ghost)
- 현재 블록이 지금 하드드롭하면 떨어질 위치를 반투명하게 미리 표시. 초보 배려 + 표준 기능.

### 1.8 넥스트 큐 & 홀드 표시
- **다음에 올 블록 5개**를 우측에 미리 보여줌.
- 홀드한 블록은 좌측 상단에 표시.

---

## 2. 점수 · 레벨 시스템

### 2.1 라인 클리어 점수 (레벨 n 기준)
| 클리어 | 이름 | 점수 |
|--------|------|------|
| 1줄 | Single | 100 × 레벨 |
| 2줄 | Double | 300 × 레벨 |
| 3줄 | Triple | 500 × 레벨 |
| 4줄 | **Tetris** | 800 × 레벨 |

- **소프트 드롭**: 칸당 +1
- **하드 드롭**: 칸당 +2
- **콤보**: 연속으로 라인을 지우면 콤보 보너스(50 × 콤보 × 레벨)
- **(선택) T-스핀**: T블록을 회전으로 끼워 라인 지우면 큰 보너스 — MVP 이후 확장 여지로 남김

### 2.2 레벨 & 속도
- **누적 라인 10줄마다 레벨 +1**(레벨 1 시작).
- 레벨이 오를수록 블록 **자동 낙하 속도가 빨라짐**(가이드라인 중력 곡선 사용): 레벨 1은 약 1초/칸, 레벨 15쯤엔 거의 즉시 낙하.
- 속도 곡선 예 (프레임/칸 → ms 환산): Lv1≈1000ms, Lv5≈400ms, Lv10≈100ms, Lv15≈30ms.

### 2.3 게임 종료
- 새 블록이 스폰될 자리가 이미 막혀 있으면(탑아웃) → **게임 오버**.
- 게임 오버 시 최종 점수/레벨/지운 줄 수를 요약 표시 → 랭킹 제출 유도.

---

## 3. 게임 모드

### 3.1 마라톤 (기본 · MVP)
- 죽을 때까지 계속. 레벨 무한 상승. **랭킹은 점수 기준.** ← snake와 동일한 뼈대.

### 3.2 (선택) 스프린트 40줄
- **40줄을 가장 빨리 지우는 타임 어택.** 랭킹은 "시간(짧을수록 위)".
- 이건 랭킹 정렬 기준이 반대라(점수 낮은 게 아니라 시간 짧은 게 위), MVP엔 빼고 2차로.

**MVP는 마라톤 단일 모드로 간다.** (versus/대전은 아래 6번 확장 참고)

---

## 4. 기술 구조

### 4.1 프론트엔드 (핵심)
```
frontend/src/app/tetris/page.tsx     — 페이지 + UI(닉네임, 랭킹 패널, 조작 버튼)
frontend/src/app/tetris/engine.ts    — 순수 게임 로직(보드/블록/회전/충돌/라인클리어/점수)
frontend/src/app/tetris/srs.ts       — SRS 킥 테이블 상수
```
- **렌더링**: `<canvas>` 또는 CSS Grid. 부드러운 낙하·고스트·라인 클리어 애니메이션을 위해 **canvas 권장**.
- **게임 루프**: `requestAnimationFrame` 기반 고정 타임스텝(누적 시간 → 중력 틱). 탭 비활성 시 자동 일시정지.
- **입력**: 키보드(PC) + 화면 버튼/스와이프(모바일) 동시 지원. 설정에서 DAS/ARR 조절 가능(고급).
- 엔진은 **프레임워크 무관 순수 함수/클래스**로 분리 → 테스트·재사용 쉬움.

### 4.2 백엔드 (최소)
- **신규 코드 거의 없음.** 기존 `ScoreService`의 화이트리스트에 `"tetris"` 한 줄만 추가:
  ```java
  private static final Set<String> GAMES = Set.of("snake", "territory", "tetris");
  ```
- 랭킹 조회: `GET /api/v1/scores/tetris?limit=10`
- 점수 제출: `POST /api/v1/scores/tetris  { nick, score }`
- `tb_score` 테이블 그대로 사용 → **DB 마이그레이션 불필요**(테이블/인덱스는 IF NOT EXISTS로 자동 관리).

### 4.3 홈 등록
`frontend/src/app/page.tsx` GAMES 배열에 추가:
```ts
{ href: '/tetris', title: '🧱 테트리스', desc: '블록을 쌓아 줄을 지우는 고전 낙하 퍼즐. 혼자 최고점수 도전 + 랭킹 등록.',
  hover: 'hit', solo: true, soloLabel: '🤖 1인 플레이' }
```

---

## 5. UI / UX (모바일·PC 둘 다 1급)

### 5.1 레이아웃
```
┌───────────────────────────────┐
│  🧱 테트리스        점수 12,340 │  ← 헤더(점수/레벨/줄수)
│                    Lv 4  Lines 38│
├──────┬──────────────┬──────────┤
│ HOLD │              │  NEXT     │
│ ┌──┐ │   플레이필드   │  ┌──┐    │
│ │  │ │   10 × 20     │  ├──┤    │
│ └──┘ │  (고스트 표시)  │  ├──┤ x5 │
│      │              │  └──┘    │
├──────┴──────────────┴──────────┤
│  ◀  ⟲   ⤓(하드)  ⟳   ▶   ⇩(소프트) │ ← 모바일 조작 바
└───────────────────────────────┘
```
- **PC**: 플레이필드 크게, HOLD/NEXT 사이드. 하단 조작 바는 숨기고 키 안내만.
- **모바일**: 세로 화면 기준. 플레이필드가 화면 60%, 하단에 **엄지로 닿는 큰 조작 버튼**. 스와이프(좌우=이동, 아래=소프트, 위=회전, 아래로 빠르게=하드드롭)도 병행.

### 5.2 조작 안내(가이드 패널)
- 첫 진입 시 규칙/조작 접이식 가이드(오목·경마에서 쓴 가시성 큰 패널 스타일 재사용). 모바일에서도 글씨 충분히 크게.

### 5.3 연출
- 라인 클리어: 번쩍 + 줄 붕괴 애니메이션.
- 테트리스(4줄): 특별 이펙트 + "TETRIS!" 텍스트.
- 레벨업: 배경 색조 변화로 속도 상승 체감.
- 하드드롭: 착지 잔상/셰이크(과하지 않게).

### 5.4 키 매핑(PC 기본)
| 키 | 동작 |
|----|------|
| ← → | 이동 |
| ↓ | 소프트 드롭 |
| Space | 하드 드롭 |
| ↑ / X | 시계 회전 |
| Z / Ctrl | 반시계 회전 |
| C / Shift | 홀드 |
| Esc / P | 일시정지 |

---

## 6. (선택) 확장 로드맵 — MVP 이후

1. **T-스핀 인식 & 보너스** — 점수 체계 고급화.
2. **스프린트 40줄 / 울트라 2분** 모드 + 모드별 랭킹.
3. **봇 대전 / 배틀(가비지 라인)**: 줄 지우면 상대에게 방해 줄 전송. 다만 이건 실시간 동기화가 필요해서 구조가 완전히 달라짐(WebSocket 필요) → 별도 대공사. 지금 폴링 구조로는 부적합하니 정말 원할 때 따로 설계.
4. 개인 기록(최고 점수/레벨) localStorage 저장.

---

## 7. 결정 필요 사항

1. **렌더링 방식**: canvas(연출 유리, 추천) vs CSS Grid(구현 단순).
2. **모드 범위**: MVP는 마라톤 단일 — OK? 스프린트도 처음부터 넣을지.
3. **T-스핀**: MVP 포함 vs 2차. (추천: 2차 — 코어부터 탄탄히)

---

## 8. 구현 순서(마일스톤)

1. **엔진 코어**(`engine.ts`): 보드/블록/7-bag/충돌/이동/라인클리어/점수 — 프레임워크 없이 순수 로직 + 단위 테스트.
2. **SRS 회전/월킥**(`srs.ts`) 붙이기.
3. **렌더 & 루프**: canvas 렌더 + requestAnimationFrame 중력 + 고스트/넥스트/홀드.
4. **입력**: 키보드 → 모바일 버튼/스와이프.
5. **연출**: 라인 클리어/테트리스/레벨업 이펙트.
6. **랭킹 연동**: 백엔드 화이트리스트 한 줄 + 제출/조회 UI(snake 페이지 패턴 복붙).
7. **홈 등록 + 가이드 패널** + 모바일 QA.
```
